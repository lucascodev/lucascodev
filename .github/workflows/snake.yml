name: Generate Snake Game
on:
  push:
    branches: [ main ]

jobs:
  generate-snake-game:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          username: lucascodev
          token: ${{ secrets.GITHUB_TOKEN }}
          output: dist
